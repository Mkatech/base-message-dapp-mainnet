{
  "name": "base-message-dapp-mainnet",
  "version": "1.0.0",
  "description": "Simple Message Board dApp deployed on Base (supports testnet & mainnet) with Hardhat + HTML frontend.",
  "scripts": {
    "test": "npx hardhat test",
    "compile": "npx hardhat compile",
    "deploy:baseSepolia": "npx hardhat run scripts/deploy.js --network baseSepolia",
    "deploy:base": "npx hardhat run scripts/deploy.js --network base"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "^16.4.0",
    "hardhat": "^2.22.0"
  }
}